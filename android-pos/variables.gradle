ext {
    minSdkVersion = 28
    compileSdkVersion = 36
    targetSdkVersion = 36
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    // S4: Keystore-backed EncryptedSharedPreferences for the SecureKV token
    // store. 1.1.0 is the first STABLE release of the MasterKey API
    // (KeyScheme.AES256_GCM); it is compatible with this minSdk 28 / AndroidX
    // stack (tink-android 1.8.0 is pulled transitively). See the S4 packet
    // report for the version rationale.
    androidxSecurityCryptoVersion = '1.1.0'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    cordovaAndroidVersion = '14.0.1'
}
