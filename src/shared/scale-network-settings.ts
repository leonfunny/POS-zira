export const DEFAULT_SCALE_SHARE_PORT = 17891;
export const SCALE_CONNECT_TIMEOUT_MS = 2000;
// A configured COM read can take 4s, plus one unstable retry and 250ms settling.
export const DEFAULT_REMOTE_SCALE_TIMEOUT_MS = 10_000;
